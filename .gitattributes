# Fins de ligne normalisées en LF dans le dépôt (cohérent multi-OS, stoppe les avertissements CRLF)
* text=auto eol=lf

# Fichiers binaires : aucune conversion
*.png  binary
*.ico  binary
*.woff2 binary
*.gguf binary
*.exe  binary
*.dll  binary
